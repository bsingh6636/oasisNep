# Gemini CLI Notes

## Important Reminders

*   **DO NOT directly edit `.env` files.** The Gemini CLI should never modify `.env` files directly. If changes are needed, the user must be instructed to make them manually. This is to prevent accidental data loss and maintain security.
